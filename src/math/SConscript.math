import os
import sys
import shutil
import subprocess

Import('buildEnv','pathDict','priorLibs','externalFlags')

dirName = 'math'

libSrc = ( \
  'arMath.cpp', \
  'arNavigationUtilities.cpp', \
  )

progNames = (
    'TestMath',
    )

# Call the generic directory-builder script
scriptPath = os.path.join( os.environ['SZGHOME'], 'build', 'scons', 'SConsBuildDir.py' )
execfile( scriptPath )

