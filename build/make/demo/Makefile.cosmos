include $(SZGHOME)/build/make/Makefile.vars

VPATH = ../../../../src/demo/cosmos

ALL = cosmos$(EXE)
Everything: $(ALL)

include $(SZGHOME)/build/make/Makefile.rules

cosmos$(EXE): cosmos$(OBJ_SUFFIX) $(SZG_LIBRARY_DEPS)
	$(SZG_USR_FIRST) cosmos$(OBJ_SUFFIX) $(SZG_USR_SECOND)
	$(COPY)
