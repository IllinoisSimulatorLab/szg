include $(SZGHOME)/build/make/Makefile.vars

VPATH = ../../../../src/demo/parade

OBJSRAW = BlobbyMan BlobbyNode ColIcosahedronMesh Commando parade
OBJS = $(OBJSRAW:%=%$(OBJ_SUFFIX))

ALL = parade$(EXE)
Everything: $(ALL)

include $(SZGHOME)/build/make/Makefile.rules

parade$(EXE): $(OBJS) $(SZG_LIBRARY_DEPS)
	$(SZG_USR_FIRST) $(OBJS) $(SZG_USR_SECOND)
	$(COPY)
